# Supabase Environment Variables
# Create a free project at https://supabase.com, then paste your credentials here.
# These values are safe to expose in a frontend app — they only allow public access.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
